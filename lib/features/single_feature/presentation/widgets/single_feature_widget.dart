import 'package:flutter/material.dart';

/// Main singleFeature flow in wallets.
///
/// This is the landing page after a user clicks on the singleFeature button.
class SingleFeatureWidget extends StatelessWidget {
  /// Creates a [SingleFeatureWidget].
  const SingleFeatureWidget({super.key});

  @override
  Widget build(BuildContext context) => Placeholder();
}
